import Game.Instance.*;
import Menu.*;

public class Main {
    public static void main(String[] args) {
        MainMenuApplication.start(args);

        // GameInstance game = new GameInstance();
        // game.start();
    }
}